local UI = script.Parent --if the script is in the GUI Frame
local button = UI.TextButton --if the button is in the GUI Frame and it's TextButton

button.MouseButton1Click:Connect(function()
    --do something
end)
